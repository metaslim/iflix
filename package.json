{
  "name": "iflix",
  "version": "1.0.0",
  "description": "iflix backend test coding experiment",
  "main": "index.js",
  "scripts": {
    "start_db": "mongod --dbpath data/db &",
    "stop_db": "ps -ef | grep mongod | grep -v grep | awk '{print \"kill -9 \"$2}' | sh",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^1.14.11"
  },
  "dependencies": {
    "express": "^4.16.2"
  }
}
